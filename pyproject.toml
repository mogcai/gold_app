[project]
name = "gold-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "mog", email = "mogcai@hotmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "polars>=1.43.2",
    "streamlit>=1.63.0",
    "yfinance>=1.6.0",
]

[project.scripts]
gold-app = "gold_app:main"

[build-system]
requires = ["uv_build>=0.12.2,<0.13.0"]
build-backend = "uv_build"
